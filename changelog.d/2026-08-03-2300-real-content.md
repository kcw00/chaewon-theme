## 2026-08-03 23:00 — Real identity, photographs, and projects

Branch: `update`.

### Context
Six changes replacing placeholder content with the real thing: avatar
mark in the nav, real contact details, new About copy, six photographs,
and ten real projects from the résumé.

### Changed
**Avatar — `assets/img/avatar.png`, `patterns/site-logo.php`**
Replaces the "portfolio" site title. The source was 1024×1024 RGBA with
transparent ground and near-black strokes; cropped square to the ink
bounding box, resized to 192px, and **every visible pixel normalised to
#000 exactly** — `invert(1)` on a #1c1a1a stroke returns a dirty grey,
not the paper colour. One file, inverted in CSS for dark mode; two files
would eventually disagree.

Shipped as a theme asset behind a PHP pattern so the path is built with
`get_template_directory_uri()`. Hard-coding `/wp-content/themes/…` in the
template part would break in a subdirectory install.

**Contact** — `kimchaewon877@gmail.com` and the real LinkedIn URL, in
both the footer social links and the contact section. The contact
section's second link changed from GitHub to LinkedIn.

**About copy** — three new paragraphs.

**Memories — `assets/img/memories/*.jpg`**
Six photographs, resized to 1200px max at q80: 1.0 MB for the set,
lazy-loaded after the first tile. Theme assets rather than media-library
attachments so they travel with the repository.

Every source photo is roughly 3:4, so the ratio modifiers create the
variety — without them the board is six near-identical portraits.
`.memory-plate` and its CSS removed.

**Projects** — the eight seeded placeholders deleted, ten real projects
created with detail pages: Production Booking Platform, Minion, AI
Telecom Customer Service Platform, Discord Bot, Portfolio Website,
PawBondAI, AI Chat App, Smart Diagnosis, YVR Traffic, C++ SDL2 Games.

Archive page size 8 → 12. Eight was chosen when the number only had to
fill an empty layout; with ten real projects it stranded two on page two.

### Verification
```
avatar     34px in nav · filter invert(1) in dark, white on the dark ground
memories   6 photos, all loaded, masonry balanced 2-2-2
           col1 482/308 · col2 385/289 · col3 308/482
projects   10 created, all URLs 200
           archive lists all 10 · homepage shows the 4 newest
           /projects/minion/ → 2026 · AI tooling · 01 The story ·
           02 What it does · 03 Built with · 5 bullets · 6 chips
contact    kimchaewon877@gmail.com · linkedin.com/in/chaewon-kim-6b145825b
```
No console errors. Database dumped to `.context/backups/` before both the
placeholder deletion and the reseed.

### Rollback
`git checkout wordpress-portfolio-theme` for the code. Content is
separate: restore `.context/backups/wordpress-pre-real-projects-*.sql`,
or delete the ten projects under Projects in wp-admin.

### For LLMs
- **Dates on the six personal projects are invented.** The résumé gives
  periods only for the four team projects — Booking Platform 2026-02,
  AI Telecom 2025-07, Smart Diagnosis 2024-09, YVR Traffic 2023-10.
  Minion, Discord Bot, Portfolio Website, PawBondAI, AI Chat App, and
  C++ SDL2 Games were dated to produce a sensible ordering and nothing
  more. The post year is printed in the chapter rule on every project
  page, so these are visible factual claims and need confirming.
- **No project has `project_url` set**, so the Visit link is hidden on
  every project page. That is the hide path working, not a bug.
- The avatar art is normalised to pure black on purpose. Re-exporting it
  from a source with near-black strokes silently degrades the dark-mode
  inversion.
- Memory photos are theme assets, so swapping one means replacing a file
  in `assets/img/memories/` — not the media picker. The alt text is
  written per photo and needs updating alongside.
- The hero tagline still reads "Software engineer — I build the systems
  I run, and run the systems I build," which predates the new About copy
  and its emphasis on looking for a team. Not part of this change.
